import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gastos.mobile',
  appName: 'GastosMobile',
  webDir: 'dist/web/browser'
};

export default config;
